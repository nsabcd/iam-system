version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: iam-redis-dev
    ports:
      - "6379:6379"
    command: redis-server --save "" --appendonly no # Disables persistence for fast in-memory dev execution
    restart: unless-stopped